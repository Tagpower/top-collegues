import { FilterByPseudoPipe } from './filter-by-pseudo.pipe';

describe('FilterByPseudoPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterByPseudoPipe();
    expect(pipe).toBeTruthy();
  });
});
